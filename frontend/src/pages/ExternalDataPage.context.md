# ExternalDataPage Context

Purpose: Fetches external user data from JSONPlaceholder and displays it in a responsive table.

Responsibilities:
- Load data from the remote API with loading and error states.
- Render the data in a clean, responsive table layout.
- Keep the page reusable and easy to extend for additional external datasets.
